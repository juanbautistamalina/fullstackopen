{
  "name": "backend-notes",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf dist && cd ../../part2/notes && npm run build && cp -r dist ../../part3/backend-notes",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"build(notes): actualizar build del frontend\" && git push",
    "lint": "eslint .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.7.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "eslint": "10.7.0",
    "globals": "^17.7.0",
    "nodemon": "^3.1.14"
  }
}
